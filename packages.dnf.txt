# sudo dnf copr enable starfall/astalRPM   (needed for the libastal-* packages below)
# sudo dnf copr enable lionheartp/Hyprland (needed for `hyprland` below - build.sh
#                                            then patches and versionlocks it, see
#                                            sources.lock/packages.other.md)
# sudo dnf install $(grep -v '^#' packages.dnf.txt)

# Hyprland itself - from the lionheartp/Hyprland COPR above, NOT stock Fedora
# repos. build.sh installs a source-patched build over this afterward; this
# package is still needed first for its runtime deps and as the baseline
# version that patch applies to.
hyprland
hyprland-guiutils
hyprland-uwsm

# Screen sharing / portals
xdg-desktop-portal
xdg-desktop-portal-hyprland
xdg-desktop-portal-gtk

# App launcher
rofi
rofi-themes

# Terminal
kitty

# Wallpaper daemon (see .config/hypr/scripts/set-wallpaper.sh)
swww

# GTK/Qt theming
kvantum
kvantum-qt5
qt6ct
gtk4-layer-shell

# Night light
gammastep

# Build deps for hyprland-preview-share-picker (see packages.other.md,
# build.sh)
gtk4-layer-shell-devel
gtk4-devel

# Build deps for ags itself (see packages.other.md, build.sh) - upstream's
# Fedora list: https://aylur.github.io/ags-docs/guide/install
meson
ninja-build
golang
nodejs
nodejs-npm
gobject-introspection-devel
gtk3-devel
gtk-layer-shell-devel

# Astal C libraries ags links against at build time. Not in core Fedora
# repos - enable the COPR first:
#   sudo dnf copr enable starfall/astalRPM
libastal-io
libastal-io-devel
libastal-4
libastal-4-devel
libastal-hyprland
libastal-hyprland-devel
libastal-tray
libastal-tray-devel
libastal-notifd
libastal-notifd-devel
libastal-apps
libastal-apps-devel
libastal-mpris
libastal-mpris-devel

# KDE-adjacent pieces this setup actually uses, that do NOT require the
# plasma-workspace/plasma-desktop shell (confirmed via `rpm -q --requires`
# against a live KDE-spin machine) - needed if installing straight onto
# Hyprland without the KDE spin/plasma-desktop metapackage.
sddm
sddm-breeze
kf6-kwallet
kwalletmanager5
polkit-kde
plasma-integration
breeze-icon-theme
breeze-icon-theme-fedora
breeze-cursor-theme

# Akonadi/PIM stack (korganizer/kontact) - also never part of the Plasma
# shell, was separately installed even on the source KDE-spin machine.
# akonadi-server-mysql pulls MariaDB as its backend; mariadb-server is
# listed explicitly since a minimal install won't provide/start it for you.
akonadi-server
akonadi-server-mysql
akonadi-mime
akonadi-contacts
akonadi-search
akonadi-calendar
akonadi-import-wizard
mailimporter-akonadi
kontactinterface
korganizer
korganizer-libs
kontact
kontact-libs
mariadb-server
